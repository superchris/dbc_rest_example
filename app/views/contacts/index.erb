<div class="container">
  <h1>Contacts</h1>
  <% @contacts.each do |contact| %>
    <ul>
      <li><%= contact.name %></li>
      <li><%= contact.email %></li>
    </ul>
    <hr>
  <% end %>
  <a href="/contacts/new">Create Contact</a>
</div>
